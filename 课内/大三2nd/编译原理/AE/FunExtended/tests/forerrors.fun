# TEST FUNCTION
# consecutive Summation

func int sum (int n):  # Return sum(0...n+)
	int f = 0
	int j = true # control var type is bool, should be int
	for i=1 to 5: # undeclared control variable errors
		f = f+1.

	for j=true to false: # expr1 and expr2 type is bool, should be int
	.

	return f .

proc main ():
# Read integers and write their factorials.
	int num = read()
    write(sum(num)) # error
.


