# TEST FUNCTION
# consecutive Summation

func int sum (int n):  # Return sum(0...n+)
	int f = 0
	int i = 0
	for i to 5: #missing '='
	.

	for i=2>1 to 5: # invalid expr1 operator would cause evaluation to bool
	.


	return f .

proc main ():
# Read integers and write their factorials.
	int num = read()
    write(sum(num)) # error
.


